# Core GUI / audio / VAD
PySide6>=6.6
sounddevice>=0.4.6
numpy>=1.26
soundfile>=0.12
silero-vad>=5.1

# Transcription engine
vosk>=0.3.45

# Model downloads (setup_models.py)
requests>=2.31
# Makes Python trust the Windows system certificate store (not just certifi's
# bundled list) -- needed on machines where an antivirus/proxy does TLS
# interception with a locally-generated root cert (e.g. Norton, Zscaler).
pip-system-certs>=4.0

# Dev / test
pytest>=8.0
